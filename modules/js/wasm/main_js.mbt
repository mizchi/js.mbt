// main_js.mbt - JS target stub (this package is WASM-GC focused)

///|
fn main {
  // This package is designed for WASM-GC target.
  // Use `moon build --target wasm-gc --release src/wasm` to build.
  @core.undefined() |> ignore
}
