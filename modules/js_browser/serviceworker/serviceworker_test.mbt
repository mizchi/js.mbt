///|
/// Service Worker API tests
/// Note: These tests only verify type definitions since Service Worker
/// requires a browser environment.

// Service Worker API is browser-only, so we cannot run full tests in Node.js.
// The bindings are provided for browser usage.
