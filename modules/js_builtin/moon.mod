name = "mizchi/js_builtin"

version = "0.12.2"

import {
  "mizchi/js_core@0.12.2",
  "moonbitlang/async@0.20.5",
}

repository = "https://github.com/mizchi/js.mbt"

license = "MIT"

keywords = [ "js", "builtins", "object", "array", "json" ]

description = "javascript built-in object bindings (Object, Array, JSON, RegExp, Symbol, Proxy, ...)"

source = "src"

preferred_target = "js"
