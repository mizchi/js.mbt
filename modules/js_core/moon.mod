name = "mizchi/js_core"

version = "0.12.2"

import {
  "moonbitlang/async@0.20.5",
}

readme = "src/README.md"

repository = "https://github.com/mizchi/js.mbt"

license = "MIT"

keywords = [ "js", "ffi", "core", "any", "promise" ]

description = "js ffi foundation for MoonBit (Any, Promise, Nullable, target-specific interop)"

source = "src"

preferred_target = "js"
