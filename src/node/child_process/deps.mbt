///|
using @js {
  js,
  safe_js,
  unsafe_cast,
  to_option,
  type Val,
  trait Js,
  type AbortSignal,
}

///|
using @events {trait EventEmitterImpl}

///|
using @stream {type ReadableStream, type WritableStream}
